<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Campgrounds List</title>
</head>
<body>
    <h1>Campgrounds List</h1>
    <ul>
        <% for (let campground of campgrounds) { %>
            <li> <a href='/campgrounds/<%= campground._id%>'><%= campground.title %></a> </li>
        <% } %>
    </ul>
    <a href="/campgrounds/new">Register campground</a>
    <a href="/">Back to Home</a>
    <form action='/campgrounds?_method=DELETE' method='POST'>
        <input style='background-color: red; color: white;' type="submit" value='delete all'>
    </form>
    
</body>
</html>